import { defineConfig } from 'vite'

// https://vitejs.dev/config/
export default defineConfig({
  // Vite's defaults are perfect for this setup.
  // It will automatically use `index.html` as the entry point
  // and process the script tag inside it.
})
